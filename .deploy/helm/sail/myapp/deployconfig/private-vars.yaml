#################  privates vars  #################
replicaCount: 1

image:
  repository: maborosii/sail
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: "latest"

